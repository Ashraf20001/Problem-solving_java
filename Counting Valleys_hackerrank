public static int countingValleys(int steps, String path) {
		    // Write your code here
		       int hight=0;
		       int valleys=0;
		       for(int i=0;i<steps;i++) {
		    	   if(path.charAt(i)=='U') {
		    		   hight++;
		    		   if(hight==0) {
		    			   valleys++;
		    		   }
		    	   }
		    	   else
		    	   {
		    		   hight--;
		    	   }
		       }
		       return valleys;
		       
		    }
